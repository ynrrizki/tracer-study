<div class="form-control">
    <label for="status" class="label">
        {{ $question->name }}
    </label><input type="date" class="input input-bordered" id="{{ $question->id }}" name="{{ $question->id }}">
</div>
